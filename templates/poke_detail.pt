<!DOCTYPE html>
<html lang="en">
<head>
    <!-- META tags -->
    <meta charset="utf-8"/>
    <meta http-equiv="content-language" content="PL">
    <!-- mobile ready -->
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Pokemon detail</title>
</head>
<body>
    <img style="width: 250px; height:250px;" src=${pokemon.sprites.front_default} />
    <img style="width: 250px; height:250px;" src=${pokemon.sprites.back_default} />
    <h1>ID:</h1>
    <h2>${pokemon.id}</h2>

    <h1>Nazwa:</h1>
    <h2>${pokemon.name}</h2>

    <h1>Typ:</h1>
    <div tal:repeat="item pokemon.types">
    <?python types = f"{item['type']['name']} " ?>
        <h2>${types}</h2>
    </div>

    <h1>Statystyki:</h1>
    <div tal:repeat="item pokemon.stats">
    <?python stat = f"{item['stat']['name']}: {item['base_stat']}" ?>
        <h2>${stat}</h2>
    </div>

    <h1>Umiejetnosci:</h1>
    <div tal:repeat="item pokemon.abilities">
    <?python ability = item['ability']['name'] ?>
        <h2>${ability}</h2>
    </div>

</body>
</html>